import { Metadata } from "next"

export const metadata: Metadata = {
  title: 'Directions',
  description: '',
  robots: {
    follow: true,
    index: true
  },
}

const Directions = () => {
  return (
    <div>Directions</div>
  )
}

export default Directions